# PostgreSQL StatefulSet and Service are managed by ArgoCD via k8s/backstage/.
# Terraform retains no resources in this module — it exists as a placeholder
# so main.tf can still reference module.postgres.* outputs for the secret.
